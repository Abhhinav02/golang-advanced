```go 
package main

import "fmt"

func main() {
	ch := make(chan int)
	rCh := make(chan int)

	// send-only channel (producer)
	go func(ch chan<- int) {
		for i := range 5 {
			ch <- i
		}
		close(ch)
	}(ch)

	for val := range ch {
		fmt.Println("🚀 Received:",val)
	}

	// receive-only channel (consumer)
	go func() {
		for i := range 5 {
			rCh <- i
		}
		close(rCh)
	}()

	receiveData(rCh)
}

// receive-only channel (consumer)
func receiveData(ch <- chan int){
	for val := range ch {
		fmt.Println("✅ Received (receive-only):",val)
	}
}

//! 💡 PRODUCER and CONSUMER pattern

```

``` 
$ go run .
🚀 Received: 0
🚀 Received: 1
🚀 Received: 2
🚀 Received: 3
🚀 Received: 4
✅ Received (receive-only): 0
✅ Received (receive-only): 1
✅ Received (receive-only): 2
✅ Received (receive-only): 3
✅ Received (receive-only): 4
```